import React from "react";
import "./PlaylistCovers.scss";

const PlaylistButtons: React.FC = () => {
  return (
    <div className="playlistButtons">
      <div className="playlistButtons__item">Playlist Cover</div>
    </div>
  );
};

export default PlaylistButtons;
